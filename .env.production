# Production — direct browser fetch if CSFloat CORS is verified (ADR-0008).
# If CORS fails, change to: VITE_PRICE_API_BASE=/.netlify/functions/price-proxy
VITE_PRICE_API_BASE=https://csfloat.com/api/v1
